name: Update Followers Count

permissions:
  contents: write  # Allow write access to repository contents

on:
  schedule:
    - cron: '0 * * * *' # Runs every hour
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4  # Using v4

      # Step 1: Fetch followers count from GitHub API
      - name: Fetch followers count from GitHub API
        run: |
          echo "Fetching follower count..."
          response=$(curl -s -w "%{http_code}" -o response.json https://api.github.com/users/K4le21)
          echo "API response code: $response"  # Log API response code for debugging

          if [ "$response" -ne 200 ]; then
            echo "Error: Failed to fetch followers. HTTP Status: $response"
            cat response.json  # Log the full response for further inspection
            exit 1
          fi

          # Read the JSON response
          followers=$(jq '.followers' response.json)
          echo "Followers count: $followers"  # Log the extracted followers count

          # Check if followers were successfully fetched
          if [ -z "$followers" ] || [ "$follow
